package com.jake.jakemod.tags;

import com.jake.jakemod.JakeMod;
import net.minecraft.sounds.SoundEvent;
import net.minecraft.tags.BlockTags;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.ForgeRegistries;

public class ModBlockTags {
   // public static final DeferredRegister<BlockTags> BLOCK_TAGS =
           // DeferredRegister.create(ForgeRegistries.BLOCK, JakeMod.MOD_ID);

 //   public static void register(IEventBus eventBus) {
   //     BLOCK_TAGS.register(eventBus);
 //   }
}
